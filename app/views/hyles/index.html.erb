<div class="sidebar">
  <%= render 'layouts/sidebar' %>
</div>
<div class="hyle-index">
  <h2><%= @title %></h2>
  <% @hyles.each do |hyle| %>
    <div class="hyle-box">
      <%= render 'hyle_box', hyle: @hyle %>
    </div>
  <% end %>
</div>
