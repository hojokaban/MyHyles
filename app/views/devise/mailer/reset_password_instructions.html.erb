<h3><%= @resource.name %>さん、こんにちは!</h3>

<p>パスワードの変更が要求されました。以下のリンクをクリックして、パスワードの再設定を行ってください。</p>

<p><%= link_to 'パスワードの変更', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>もしこのメールに見覚えがない場合は、無視してください。</p>
<p>上のリンクから新しいパスワードが作成されない限り、パスワードが変更させることはありません。</p>
